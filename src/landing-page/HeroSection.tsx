
/** @jsx jsx */
import { css, jsx } from '@emotion/react'
import React, { ReactPropTypes, useState } from "react";

export default function Example() {
  return (
    <React.Fragment>
      <div className="subtitle">
        Ready to use UI Components crafted with TailwindCSS
      </div>
    </React.Fragment>
  )
}